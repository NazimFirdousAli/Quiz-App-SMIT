const category = () => {
    var arr = new Array();
    firebase.database().ref("QUIZ").on('child_added', function (data) {
        const fun = async() => {
            
        }
    });
}
category();